import {Banknote,CalendarDays,FileText,LockKeyhole} from "lucide-react";import {Badge} from "@/components/ui/badge";import {Card,CardContent,CardDescription,CardHeader,CardTitle} from "@/components/ui/card";
type Slip={id:string;period:string;paymentDate:string;gross:number;deductions:number;net:number;payableDays:number;lopDays:number;status:string;publishedAt:string};const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:2}).format(value);
export function EmployeePayroll({employeeName,slips}:{employeeName:string;slips:Slip[]}){return <div className="space-y-6"><div><p className="text-sm font-medium text-primary">Compensation</p><h2 className="mt-1 text-2xl font-semibold">My Payroll</h2><p className="mt-1 text-sm text-muted-foreground">{employeeName} · View your published salary history and payout details.</p></div><div className="grid gap-3 sm:grid-cols-3">{[["Latest net pay",slips[0]?money(slips[0].net):"—",Banknote],["Published slips",String(slips.length),FileText],["Access","Private & secure",LockKeyhole]].map(([a,b,Icon])=>{const C=Icon as typeof Banknote;return <Card key={String(a)}><CardContent className="flex items-center gap-3 p-4"><span className="flex size-10 items-center justify-center rounded-xl bg-accent text-primary"><C/></span><div><p className="font-semibold">{String(b)}</p><p className="text-xs text-muted-foreground">{String(a)}</p></div></CardContent></Card>})}</div><Card><CardHeader><CardTitle>Salary slips</CardTitle><CardDescription>Only published, immutable payroll versions are visible here.</CardDescription></CardHeader><CardContent className="space-y-3">{slips.map(slip=><details key={slip.id} className="group rounded-xl border"><summary className="flex cursor-pointer list-none items-center gap-3 p-4"><span className="flex size-10 items-center justify-center rounded-lg bg-emerald-50 text-emerald-700"><FileText/></span><span className="min-w-0 flex-1"><b>{slip.period}</b><span className="block text-xs text-muted-foreground">Payment date {slip.paymentDate}</span></span><span className="text-right"><b className="block">{money(slip.net)}</b><Badge className="bg-emerald-50 text-emerald-700">Published</Badge></span></summary><div className="grid gap-3 border-t bg-muted/20 p-4 sm:grid-cols-3">{[["Gross earnings",money(slip.gross)],["Total deductions",money(slip.deductions)],["Net pay",money(slip.net)],["Payable days",slip.payableDays],["LOP days",slip.lopDays],["Published",new Date(slip.publishedAt).toLocaleDateString("en-IN")]].map(([a,b])=><div key={a} className="rounded-lg bg-card p-3"><p className="text-xs text-muted-foreground">{a}</p><p className="mt-1 font-semibold">{b}</p></div>)}</div></details>)}{!slips.length&&<div className="py-16 text-center"><CalendarDays className="mx-auto text-muted-foreground"/><p className="mt-3 font-medium">No published salary slips</p><p className="text-sm text-muted-foreground">Approved salary slips will appear here after payroll publishes them.</p></div>}</CardContent></Card></div>}
